import { AppShell } from "@/components/app-shell";
import { LocalQuestionValidation } from "@/components/exam/local-question-validation";

export default function LocalQuestionValidationPage() {
  return (
    <AppShell>
      <LocalQuestionValidation />
    </AppShell>
  );
}